name: Phase 3 - Configure Build Linting

on:
  push:
  workflow_dispatch:

defaults:
  run:
    shell: pwsh

jobs:
  build_on_win11:
    runs-on: [self-hosted, Windows, x64]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify PowerShell
        shell: pwsh
        run: |
          Write-Host "PowerShell Version:"
          $PSVersionTable
          pwsh --version

      - name: Verify Tools on Win11
        shell: pwsh
        run: |
          git --version
          python --version
          pip --version
          cargo --version
          rustc --version
          cmake --version
          clang --version
          clang-tidy --version
          ninja --version

      - name: Python - Lint & Test
        run: |
          python -m pip install black ruff pytest
          python -m black --check python_app
          python -m ruff check python_app
          python -m pytest python_app/tests

      - name: Rust - Format, Lint, Test
        working-directory: rust_collector
        run: |
          cargo fmt -- --check
          cargo clippy -- -D warnings
          cargo test
          cargo build --release

      - name: C++ - Build with MSVC
        run: |
          cmd /c '"C:\Program Files\Microsoft Visual Studio\18\Community\Common7\Tools\VsDevCmd.bat" -arch=x64 && cmake -S cpp_app -B build/cpp -G Ninja -DCMAKE_EXPORT_COMPILE_COMMANDS=ON && cmake --build build/cpp --verbose && ctest --test-dir build/cpp --output-on-failure'
          cmake -S cpp_app -B build/cpp -G Ninja
          cmake --build build/cpp

      - name: C++ - clang-tidy
        run: |
          clang-tidy cpp_app/src/main.cpp -- -Icpp_app/include
            
      - name: Upload Rust & CPP artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rust-binary-${{ runner.os }}
          path: |
            rust_collector/target/release/rust_collector*
            cpp_app/build/baseball_telemetry_cpp.exe

  build_on_linux:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify PowerShell
        shell: pwsh
        run: |
          Write-Host "PowerShell Version:"
          $PSVersionTable
          pwsh --version

      - name: Verify Tools on Linux
        shell: bash
        run: |
          git --version
          python3 --version
          pip --version
          cargo --version
          rustc --version
          cmake --version
          clang --version
          clang-tidy --version
          ninja --version
          gcc --version
          qemu-system-x86_64 --version
          qemu-system-arm --version
          qemu-system-aarch64 --version
          qemu-img --version

      - name: Python - Lint & Test
        run: |
          python3 -m pip install black ruff pytest
          python3 -m black --check python_app
          python3 -m ruff check python_app
          python3 -m pytest python_app/tests

      - name: Rust - Format, Lint, Test
        working-directory: rust_collector     
        run: |
          cargo fmt -- --check
          cargo clippy -- -D warnings
          cargo test
          cargo build --release

      - name: Build C++
        shell: pwsh
        run: |
          cmd /c "C:\Program Files\Microsoft Visual Studio\18\Community\Common7\Tools\VsDevCmd.bat" -arch=x64
          cmake --build build/cpp --verbose 
          cmake -S cpp_app -B build/cpp -G Ninja
          cmake --build build/cpp

      - name: C++ - clang-tidy
        run: |
          clang-tidy cpp_app/src/main.cpp -- -Icpp_app/include
      
      - name: Upload Rust & artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rust-binary-${{ runner.os }}
          path: |
            rust_collector/target/release/rust_collector*
            cpp_app/build/baseball_telemetry_cpp       
 





